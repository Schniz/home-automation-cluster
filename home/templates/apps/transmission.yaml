---

# {{ $namespace := "transmission" }}
# {{- $subdomain := .Values.apps.transmission.subdomain -}}
# {{- $domain := print $subdomain "." .Values.ingress.base_domain -}}

---

apiVersion: v1
kind: Namespace
metadata:
  name: {{ $namespace }}

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: "transmission-library-storage"
  labels:
    type: "local"
    hagever.com/for: transmission
spec:
  storageClassName: manual
  capacity:
    storage: "500Gi"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.external_storage.base }}/library"

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: "transmission-config-storage"
  namespace: {{ $namespace }}
  labels:
    type: "local"
    hagever.com/for: transmission
spec:
  storageClassName: manual
  capacity:
    storage: "1Gi"
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "{{ .Values.external_storage.base }}/configs/transmission"

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ $namespace }}
  name: "transmission-config-storage"
spec:
  storageClassName: manual
  selector:
    matchLabels:
      hagever.com/for: transmission
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "1Gi"

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ $namespace }}
  name: "transmission-library-storage"
spec:
  storageClassName: manual
  selector:
    matchLabels:
      hagever.com/for: transmission
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "500Gi"

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: transmission
  namespace: {{ $namespace }}
  labels:
    app: transmission
    hagever.com/version: '1'
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: transmission
  template:
    metadata:
      labels:
        app: transmission
    spec:
      volumes:
        - name: transmission-library-storage
          persistentVolumeClaim:
            claimName: transmission-library-storage
        - name: transmission-config-storage
          persistentVolumeClaim:
            claimName: transmission-config-storage
      containers:
        - name: server
          image: linuxserver/transmission:latest
          imagePullPolicy: Always
          ports:
            - name: web-ui
              containerPort: 9091
            - name: torrent-tcp
              containerPort: 51413
            - name: torrent-udp
              protocol: UDP
              containerPort: 51413
          volumeMounts:
            - mountPath: /downloads
              name: transmission-library-storage
              subPath: downloads
            - mountPath: /config
              name: transmission-config-storage
            - mountPath: /watch
              name: transmission-library-storage
              subPath: transmission-watch
          env:
            - name: PUID
              value: "1000"
            - name: PGID
              value: "1000"
            - name: TZ
              value: Asia/Jerusalem
          livenessProbe:
            httpGet:
              path: /transmission/web/
              port: 9091
            initialDelaySeconds: 3
            periodSeconds: 3

---

apiVersion: v1
kind: Service
metadata:
  name: transmission-peers
  namespace: {{ $namespace }}
  labels:
    app: transmission
spec:
  type: NodePort
  selector:
    app: transmission
  ports:
    - protocol: TCP
      port: 31913
      nodePort: 31913
      name: torrent-tcp
    - protocol: UDP
      port: 31913
      nodePort: 31913
      name: torrent-udp

---

apiVersion: v1
kind: Service
metadata:
  name: transmission
  namespace: {{ $namespace }}
  labels:
    app: transmission
spec:
  selector:
    app: transmission
  ports:
    - protocol: TCP
      targetPort: 9091
      name: web-ui
      port: 80

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: transmission
  namespace: {{ $namespace }}
spec:
  tls:
    - hosts:
        - {{ $domain | quote }}
  rules:
  - host: {{ $domain | quote }}
    http:
      paths:
      - backend:
          service:
            name: transmission
            port:
              number: 80
        path: /
        pathType: Prefix
